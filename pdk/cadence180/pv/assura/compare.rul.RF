;/*************************************************************************
;
; GENERIC PDK Process Assura LVS Rules v2.0 1/10/02
;
; Assura LVS Command File
;
; Title:  Assura LVS Rules for
;         GENERIC PDK Process
;         Use with GENERIC PDK version 2.0
;
; Notes:  Cadence Design Systems, Inc.
;         PDK Technology Center, Melbourne, FL
;
; Reference Documents:
;
;   Library Specification No. GPDK, Version 2.0
;
;*************************************************************************
;
; Support Information:
;
; This rule file is supported by Cadence Design Systems customer support.
; For contact information consult http://www.cadence.com/support/
;
;*************************************************************************
;
; Revision History:
;
;        01/10/02  mo     Second Customer Release, Version 2.0
;                         -Updated rule file to new standards
;
;        11/05/01         First Customer Release, Version 1.0
;
;*************************************************************************
;
;  _______________________________________________________________
; * Cadence Design Systems shall not be liable for the accuracy   *
; * of this extract rule file or it's ability to capture errors.  *
; * The user is responsible for thoroughly testing and            *
; * implementing it's features.                                   *
; *---------------------------------------------------------------*
;
;
;************************************************************************/

avCompareRules(

  fileVer  = "2.0"
  fileDate = "May 7, 2002"
  libType  = "GENERIC PDK Process"
  libVer   = "version 1.7 (05/07/2002)"
  refDoc1  = "Library Specification No. GPDK, Version 1.7"

  printf( "  \n" )
  printf(
   "*************************************************************** \n"
  )
  printf( "  \n" )
  printf(
    " GENERIC PDK Assura Compare Rules file version %s\n"
    fileVer
  )
  printf( "  Cadence Design Systems, Inc. \n" )
  printf( "  PDK Technology Center, Melbourne, FL \n" )
  printf( "  %s \n" fileDate )
  printf( "  Use with %s %s\n" libType libVer )
  printf( "  \n" )
  printf( " Reference Documents: \n" )
  printf( "  %s \n" refDoc1 )
  printf( "  \n" )
  printf( " NOTICE: \n" )
  printf(
   "  Cadence Design Systems shall not be liable for the accuracy \n"
  )
  printf(
   "  of this LVS rule file or its ability to capture errors.     \n"
  )
  printf(
   "  The user is responsible for thoroughly testing and          \n"
  )
  printf(
   "  implementing its features.                                  \n"
  )
  printf( " \n" )
  printf(
   "*************************************************************** \n"
  )
  printf( " \n" )


; *********************************************************************
; Put tolerance values up front so they are easy to change
; *********************************************************************

; these are percentages

; MOS width, length

  mosW_lvsTol = 1.0
  mosL_lvsTol = 1.0

; RES w, l

  resW_lvsTol = 1.0
  resL_lvsTol = 1.0

; BJT area

  bjtAREA_lvsTol = 1.0

; DIODE area

  diodeAREA_lvsTol = 5.0

; MOSCAP w, l

  moscapW_lvsTol = 1.0
  moscapL_lvsTol = 1.0

; MIMCAP area

  mimcapAREA_lvsTol = 1.0


; *********************************************************************
; Define how devices are permuted (combined)
; *********************************************************************

; *****************************************************************
; Combine parallel MOS devices
; combines only if lengths are equal

  procedure( parallelMOS( mos1 mos2 )
    prog( ( parMos missing1 missing2 )

; set the output plist to nil

      parMos = ncons(nil)

; If m is not defined, set m to 1

      when( !mos1->m  mos1->m = float(1) )
      when( !mos2->m  mos2->m = float(1) )

; If W or L is missing, do not combine

      foreach( prop '( w l )
        unless( get( mos1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( mos2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Parallel MOS with missing parameters not combined:\n"
        )

        when( missing1
          printf(
            "   1st device: MOS missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: MOS with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: MOS missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: MOS with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that w, l and m are defined for both plists

; set variables to make typing easier and code simpler

      W1 = float( mos1->w )
      W2 = float( mos2->w )
      L1 = float( mos1->l )
      L2 = float( mos2->l )
      M1 = float( mos1->m )
      M2 = float( mos2->m )

; combine only if lengths are equal

      diffL = abs( L2 - L1 )

      unless( diffL < .005e-6
        return( "doNotCombine" )
      )

; Now combine parameters; make sure we set new L and M.

      parMos->w = float(W1 * M1 + W2 * M2)

      parMos->m = float(1.0)
      parMos->l = L1

; Report combining to the log file

      printf( "\n" )
      printf( "** Parallel MOS combined: \n" )

      printf("   1st device: L = %g W = %g m = %g \n"
        L1 W1 M1 )

      printf("   2nd device: L = %g W = %g m = %g \n"
        L2 W2 M2 )

      printf(
        "   Resulting: L = %g W = %g m = 1 \n" L1 parMos->w )

      return( parMos )

    )
  ) ;parallelMOS


; *****************************************************************
; combine parallel resistors

  procedure( parallelRES( res1 res2 )
    prog( (parRes missing1 missing2)

; set the output plist to nil
      parRes = ncons(nil)

; If m (m factor) is missing, set it to 1

      when( !res1->m  res1->m = 1 )
      when( !res2->m  res2->m = 1 )

; If W or L is missing, do not combine

      foreach( prop '( w l )
        unless( get( res1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( res2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Parallel RES with missing parameters not combined:\n"
        )

        when( missing1
          printf(
            "   1st device: Res missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: Res with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: Res missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: Res with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that w, l and m are defined for both plists

; set variables to make typing easier and code simpler

      M1 = float( res1->m )
      M2 = float( res2->m )
      W1 = float( res1->w )
      W2 = float( res2->w )
      L1 = float( res1->l )
      L2 = float( res2->l )

; Combine only if difference in width is < 0.005 um

      diffW = abs( W2 - W1 )

      when( diffW >= 0.005e-6
        printf( "\n" )
        printf(
          "** Parallel RES with unequal W not combined\n" )
        printf("   1st device: W = %g L = %g\n" W1 L1 )
        printf("   2nd device: W = %g L = %g\n" W2 L2 )

        return("doNotCombine")
      )

; combine parameters
; correct for m-factor here

      L1m  = L1/M1
      L2m  = L2/M2
      newL = (L1m * L2m) / (L1m + L2m)
      newW = W1

      parRes->w = newW
      parRes->l = newL
      parRes->m = float( 1 )

      printf( "\n" )
      printf( "** Parallel resistors combined: \n" )
      printf( "   1st device: W = %g L = %g \n" W1 L1 )
      printf( "   2nd device: W = %g L = %g \n" W2 L2 )

      printf(
        "   Resulting: W = %g L = %g m = 1\n" newW newL
      )

      return(parRes)

    ) ;prog
  ) ;procedure( parallelRES )


; *****************************************************************
; combine series resistors

  procedure( seriesRES( res1 res2 )
    prog( (serRes missing1 missing2)

; set the output plist to nil

      serRes = ncons(nil)

; If m is missing, set it to 1

      when( !res1->m  res1->m = 1 )
      when( !res2->m  res2->m = 1 )

; If W or R is missing, do not combine

      foreach( prop '( w l )
        unless( get( res1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( res2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Series RES with missing parameters not combined:\n"
        )

        when( missing1
          printf(
            "   1st device: Res missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: Res with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: Res missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: Res with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that w, r and m are defined for both plists

; set variables to make typing easier and code simpler

      M1  = float( res1->m )
      M2  = float( res2->m )
      W1  = float( res1->w )
      W2  = float( res2->w )
      L1  = float( res1->l )
      L2  = float( res2->l )

; Combine only if difference in width is < 0.005 um

      diffW = abs( W2 - W1 )

      when( diffW >= 0.005e-6
        printf( "\n" )
        printf(
          "** Series RES with unequal W not combined\n" )
        printf("   1st device: W = %g L = %g\n" W1 L1 )
        printf("   2nd device: W = %g L = %g\n" W2 L2 )

        return("doNotCombine")
      )

; combine parameters
; method depends on whether the widths or lengths are equal
; correct for m-factor here

      L1m  = L1/M1
      L2m  = L2/M2

      newW = W1
      newL = L1m + L2m

      serRes->w = newW
      serRes->l = newL
      serRes->m = 1

      printf( "\n" )
      printf( "** Series resistors combined: \n" )

      printf("   1st device: W = %g L = %g \n" W1 L1 )
      printf("   2nd device: W = %g L = %g \n" W2 L2 )

      printf("   Resulting: W = %g L = %g m = 1\n" newW newL )

      return(serRes)

    ) ;prog
  ) ;procedure( seriesRES )


; *****************************************************************
; combine parallel moscaps
; moscaps with different l's are not combined

  procedure( parallelMOSCAP( moscap1 moscap2 )
    prog( (parCap missing1 missing2)

; set the output plist to nil

      parCap = ncons(nil)

; If m (m factor) is missing, set it to 1

      when( !moscap1->m  moscap1->m = 1 )
      when( !moscap2->m  moscap2->m = 1 )

; If W or L is missing, do not combine

      foreach( prop '( w l )
        unless( get( moscap1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( moscap2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Parallel MOSCAP with missing parameters not combined:\n"
        )

        when( missing1
          printf(
            "   1st device: moscap missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: moscap with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: moscap missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: moscap with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that w, l and m are defined for both plists

; set variables to make typing easier and code simpler

      M1 = float( moscap1->m )
      M2 = float( moscap2->m )
      W1 = float( moscap1->w )
      W2 = float( moscap2->w )
      L1 = float( moscap1->l )
      L2 = float( moscap2->l )

; Combine only if difference in length is < 0.005 um

      diffL = abs( L2 - L1 )

      when( diffL >= 0.005e-6
        printf( "\n" )
        printf("** Parallel moscap with unequal L not combined\n" )

        printf("   1st device: L = %g W = %g \n" L1 W1 )
        printf("   2nd device: L = %g W = %g \n" L2 W2 )

        return("doNotCombine")
      )

; combine parameters
; correct for m-factor here

      newW   = W1 * M1 + W2 * M1
      newL   = L1

      parCap->w = newW
      parCap->l = newL
      parCap->m = float(1)

; Report combining to the log file

      printf( "\n" )
      printf( "** Parallel moscap combined \n" )

      printf("   1st device: L = %g W = %g \n" L1 W1 )
      printf("   2nd device: L = %g W = %g \n" L2 W2 )

      printf(
        "   Resulting: L = %g W = %g m = 1 \n" newL newW )

      return(parCap)

    ) ;prog
  ) ;procedure( parallelMOSCAP )


; *****************************************************************
; combine parallel MIM caps

  procedure( parallelMIMCAP( mimcap1 mimcap2 )
    prog( (parArea missing1 missing2)
  
; set the output plist to nil

      parArea = ncons(nil)

; If m (m factor) is missing, set it to 1

      when( !mimcap1->m  mimcap1->m = 1 )
      when( !mimcap2->m  mimcap2->m = 1 )

; If C is missing, do not combine

      foreach( prop '( area )
        unless( get( mimcap1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( mimcap2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        sprintf( msg
          "Parallel MIMCAPs with missing parameters not combined"
        )
        printf( "** %s: \n" msg )

        when( missing1
          printf(
            "   1st device: mimcap missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: mimcap with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: mimcap missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: mimcap with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that area and m are defined for both plists

; set variables to make typing easier and code simpler

      M1 = float( mimcap1->m )
      M2 = float( mimcap2->m )
      AREA1 = float( mimcap1->area )
      AREA2 = float( mimcap2->area )

; combine parameters
; correct for m-factor here

      newAREA   = AREA1 * M1 + AREA2 * M2

      parArea->area = newAREA
      parArea->m = float(1)

; Report combining to the log file

      printf( "\n" )
      printf( "** Parallel mimcap combined \n" )

      printf("   1st device: AREA = %g \n" AREA1 )
      printf("   2nd device: AREA = %g \n" AREA2 )

      printf(
        "   Resulting: AREA = %g m = 1 \n" newAREA )

      return(parArea)

    ) ;prog
  ) ;procedure( parallelMIMCAP )


; *****************************************************************
; Combine parallel Bipolar
; The only parameters are area, m

  procedure( parallelBJT( bjt1 bjt2 )
    prog( ( parBjt missing1 missing2 )

; set the output plist to nil

      parBjt = ncons(nil)

; If m (m factor) is missing, set it to 1

      when( !bjt1->m  bjt1->m = 1 )
      when( !bjt2->m  bjt2->m = 1 )

; If area is missing, do not combine
; foreach is not necessary since there is only one parameter
; the generic form is kept for easy expansion

      foreach( prop '( area )
        unless( get( bjt1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( bjt2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Parallel BJT with missing area not combined:\n"
        )

        when( missing1
          printf(
            "   1st device: BJT missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: BJT with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: BJT missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: BJT with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that area and m are defined for both plists

; set variables to make typing easier and code simpler

      area1 = float( bjt1->area )
      area2 = float( bjt2->area )
      M1 = float( bjt1->m )
      M2 = float( bjt2->m )

; combine only if areas per device are equal

      diffA = abs( area2 - area1 )

      unless( diffA < .001
        return( "doNotCombine" )
      )

; Now combine parameters

; save the area per emitter as the new area
; save the sum of M1 and M2 as the new m

; This is necessary because we can have more than two
;   devices in parallel; if we just saved the total area
;   we could combine the first two devices but not three.

      parBjt->area = area2
      newM = M1 + M2
      parBjt->m = newM

      printf( "\n" )
      printf( "** Parallel BJT combined: \n" )

      printf( "   1st device: area = %g m = %g \n" area1 M1 )
      printf( "   2nd device: area = %g m = %g \n" area2 M2 )

      printf( "   Resulting: area = %g m = %g \n" area2 newM )

      return(parBjt)

    ) ;prog
  ) ;procedure


; *****************************************************************
; combine parallel diodes

  procedure( parallelDIO( dio1 dio2 )
    prog( ( parDio missing1 missing2 )

; set the output plist to nil
      parDio = ncons(nil)

; If m (m factor) is missing, set it to 1

      when( !dio1->m  dio1->m = 1 )
      when( !dio2->m  dio2->m = 1 )

; If area is missing, do not combine
; foreach is not necessary since there is only one parameter
; the generic form is kept for easy expansion

      foreach( prop '( area )
        unless( get( dio1 prop )
          missing1 = cons( prop missing1 )
        ) ;unless
        unless( get( dio2 prop )
          missing2 = cons( prop missing2 )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missing1 || missing2 )

        printf( "\n" )
        printf(
          "** Parallel DIODE with missing area not combined:\n" )

        when( missing1
          printf(
            "   1st device: Diode missing parameters: %s \n"
            buildString( missing1 ",") )
        ) ;when
        when( !missing1
          printf(
            "   1st device: Diode with all parameters \n" )
        ) ;when

        when( missing2
          printf(
            "   2nd device: Diode missing parameters: %s \n"
            buildString( missing2 ",") )
        ) ;when
        when( !missing2
          printf(
            "   2nd device: Diode with all parameters \n" )
        ) ;when

        return( "doNotCombine" )
      )

; now we know that area and m are defined for both plists

; set variables to make typing easier and code simpler

      area1 = float( dio1->area )
      area2 = float( dio2->area )
      M1 = float( dio1->m )
      M2 = float( dio2->m )

; Now combine parameters; set new m = 1

      newarea = area1*M1 + area2*M2
      parDio->area = newarea
      parDio->m = float(1)

      printf( "\n" )
      printf( "** Parallel Diode combined: \n" )

      printf( "   1st device: area = %g m = %g \n" area1 M1 )
      printf( "   2nd device: area = %g m = %g \n" area2 M2 )

      printf( "   Resulting: area = %g m = 1 \n" newarea )

      return( parDio )

    ) ;prog
  ) ;parallelDIO


; *********************************************************************
; Define how device parameters are compared
; *********************************************************************

; *****************************************************************
; Compare MOS device properties

  procedure( compareMOS( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m (m factor) is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If W or L is missing generate a parameter mismatch

      foreach( prop '( w l )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch MOS missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch MOS W/L/m %g %g %g"
            float(schPlist->w) float(schPlist->l) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay MOS missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay MOS W/L/m %g %g %g"
            float(layPlist->w) float(layPlist->l) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that w, l and m are defined for both plists

      layM = layPlist->m
      layW = float( layM * layPlist->w )
      layL = float( layPlist->l )
      schM = schPlist->m
      schW = float( schM * schPlist->w )
      schL = float( schPlist->l )

      diffW = 100 * (abs( layW - schW ) / schW )
      diffL = 100 * (abs( layL - schL ) / schL )

      Werr = nil
      Lerr = nil

      when( diffW > mosW_lvsTol Werr = t )
      when( diffL > mosL_lvsTol Lerr = t )

; if no errors return nil

      when( !Werr && !Lerr  return(nil) )

; now we know there is a parameter error

      sprintf(
        msg
        "Err: MOS Sch L/W %g %g; Lay L/W %g %g"
        schL schW layL layW
      )
      return( msg )

   ) ; end prog
  ) ; end compareMOS


; *****************************************************************
; Compare resistor properties

  procedure( compareRES( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m (m factor) is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If L or W is missing generate a parameter mismatch

      foreach( prop '( l w )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch RES missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch RES L/W/m %g %g %g"
            float(schPlist->l) float(schPlist->w) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay RES missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay RES L/W/m %g %g %g"
            float(layPlist->l) float(layPlist->w) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that w, l and m are defined for both plists
; correct for m here

      layM = layPlist->m
      layL = float( layPlist->l / layM )
      layW = float( layPlist->w )

      schM = schPlist->m
      schL = float( schPlist->l / schM )
      schW = float( schPlist->w )

      diffL = 100 * (abs( layL - schL ) / schL )
      diffW = 100 * (abs( layW - schW ) / schW )

      Werr = nil
      Lerr = nil

      when( diffL > resL_lvsTol Lerr = t )
      when( diffW > resW_lvsTol Werr = t )

; if no errors return nil

      when( !Werr && !Lerr return(nil) )

; now we know there is a parameter error

      sprintf( msg "Err: RES Sch L/W %g %g; Lay L/W %g %g"
        schL schW layL layW )
      return( msg )

   ) ; end prog
  ) ; end compareRES


; *****************************************************************
; Compare MOSCAP properties

  procedure( compareMOSCAP( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m (m factor) is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If W or L is missing generate a parameter mismatch

      foreach( prop '( w l )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch moscap missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch moscap W/L/m %g %g %g"
            float(schPlist->w) float(schPlist->l) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay moscap missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay moscap W/L/m %g %g %g"
            float(layPlist->w) float(layPlist->l) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that w, l and m are defined for both plists
; correct for m here

      layM = layPlist->m
      layW = float( layM * layPlist->w )
      layL = float( layPlist->l )

      schM = schPlist->m
      schW = float( schM * schPlist->w )
      schL = float( schPlist->l )

      diffW = 100 * (abs( layW - schW ) / schW )
      diffL = 100 * (abs( layL - schL ) / schL )

      Werr = nil
      Lerr = nil

      when( diffW > moscapW_lvsTol  Werr = t )
      when( diffL > moscapL_lvsTol  Lerr = t )

; if no errors return nil

      when( !Werr && !Lerr  return(nil) )

; now we know there is a parameter error

      sprintf(
        msg
        "Err: moscap Sch L/W %g %g; Lay L/W %g %g"
        schL schW layL layW
      )
      return( msg )

   ) ; end prog
  ) ; end compareMOSCAP


; *****************************************************************
; Compare MIMCAP properties

  procedure( compareMIMCAP( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m (m factor) is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If AREA is missing generate a parameter mismatch

      foreach( prop '( area )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch mimcap missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch mimcap area/m %g %g"
            float(schPlist->area) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay mimcap missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay mimcap area/m %g %g %g"
            float(layPlist->area) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that area and m are defined for both plists
; correct for m here

      layM = layPlist->m
      layAREA = float( layM * layPlist->area )

      schM = schPlist->m
      schAREA = float( schM * schPlist->area )

      diffAREA = 100 * (abs( layAREA - schAREA ) / schAREA )

      AREAerr = nil

      when( diffAREA > mimcapAREA_lvsTol  AREAerr = t )

; if no errors return nil

      when( !AREAerr  return(nil) )

; now we know there is a parameter error

      sprintf(
        msg
        "Err: mimcap Sch AREA %g; Lay AREA %g" schAREA layAREA
      )
      return( msg )

   ) ; end prog
  ) ; end compareMIMCAP


; *****************************************************************
; Compare BJT device properties

  procedure( compareBJT( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If area is missing generate a parameter mismatch

      foreach( prop '( area )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch BJT missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch BJT Area %g m %g"
            float(schPlist->area) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay BJT missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay BJT Area %g m %g"
            float(layPlist->area) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that area and m are defined for both plists
; correct for m here

      layM = layPlist->m
      layA = float( layM * layPlist->area )
      schM = schPlist->m
      schA = float( schM * schPlist->area )

      diffA = 100 * (abs( layA - schA ) / schA )

      areaErr = nil

      when( diffA > bjtAREA_lvsTol areaErr = t )

; if no errors return nil

      when( !areaErr return(nil) )

; now we know there is a parameter error

; if m = 1 for both devices keep the message simple

      when( schM == 1 && layM == 1
        sprintf(
          msg
          "Err: BJT Sch Area %g; Lay Area %g"
          schA layA
        )
        return( msg )
      )

; m > 1 for either the schematic or layout device
; we want a different message if m=1 vs. m>1

      sprintf( msg0 "Err: BJT" )

      sprintf( msgS
        "Sch area %g (m=1)" schA )
      sprintf( msgL
        "Lay area %g (m=1)" layA )

; override these strings if m > 1

      when( schM > 1
        sprintf( msgS
          "Sch area (total) %g (m=%g)" schA float(schM) )
      )
      when( layM > 1
        sprintf( msgL
          "lay area (total) %g (m=%g)" layA float(layM) )
      )

      sprintf( msg "%s %s; %s" msg0 msgS msgL )
      return( msg )

   ) ; end prog
  ) ; end compareBJT


; *****************************************************************
; Compare ind properties

  procedure( compareIND( layPlist, schPlist )
    prog( (missingLay missingSch)

; If ind is missing generate a parameter mismatch

      foreach( prop '( ind )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch Ind missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch Ind %g"
            float(schPlist->ind)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay Ind missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay Ind %g"
            float(layPlist->ind)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that ind are defined for both plists

      layIND   = float( layPlist->ind  )

      schIND   = float( schPlist->ind )

      INDerr  = nil

      when( layIND   != schIND INDerr = t )

; if no errors return nil

      when( !INDerr return(nil) )

; now we know there is a parameter error

      sprintf( msg "Err: Ind Sch ind %g;
        Lay ind %g"
        schIND
        layIND)
      return( msg )

   ) ; end prog
  ) ; end compareIND


; *****************************************************************
; Compare diode properties

  procedure( compareDIO( layPlist, schPlist )
    prog( (missingLay missingSch)

; If m is missing, set it to 1

      when( !layPlist->m  layPlist->m = 1 )
      when( !schPlist->m  schPlist->m = 1 )

; If "area" is missing generate a parameter mismatch

      foreach( prop '( area )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch DIODE missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch DIODE Area %g m %g"
            float(schPlist->area) float(schPlist->m)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay DIODE missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay DIODE area %g m %g"
            float(layPlist->area) float(layPlist->m)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that area and m are defined for both plists
; correct for m here

      layM = layPlist->m
      layA = float( layM * layPlist->area )
      schM = schPlist->m
      schA = float( schM * schPlist->area )

      diffA = 100 * (abs( layA - schA ) / schA )

      areaErr = nil

      when( diffA > diodeAREA_lvsTol areaErr = t )

; if no errors return nil

      when( !areaErr return(nil) )

; now we know there is a parameter error

      sprintf( msg "Err: DIODE Sch Area %g; Lay Area %g" schA layA )
      return( msg )

   ) ; end prog
  ) ; end compareDIO

; *****************************************************************
; Compare VeloceRF inductors properties

procedure( compareINDVeloceRF( layPlist, schPlist )
 prog( ( missingLay missingSch rect )

      errorCount  = 0

      tolWC  = 1.0
      tolSC  = 3.5
      tolNOT = 0.0
      tolOD  = 1.0
      tolX   = 1.0
      tolY   = 1.0

      layx = nil
      layy = nil
      schx = nil
      schy = nil
      
      when( layPlist->x && layPlist->y 
        layx = layPlist->x
        layy = layPlist->y
      )
      when( schPlist->x && schPlist->y 
        schx = schPlist->x
        schy = schPlist->y
      )      

      layShape = "IND (no shape)"
      schShape = "IND (no shape)"

      if( schPlist->Size_Y
      	schShape = "rect"
      )
      if( schPlist->Int_Terminal && schPlist->Size_Y
      	schShape = "diffi"
      )
      if( schPlist->Int_Terminal && !schPlist->Size_Y
      	schShape = "octdiffi"
      )
      if( schPlist->Layer_Name && schPlist->Size_Y
      	schShape = "ltccrect"
      )
      if( schPlist->Layer_Name && !schPlist->Size_Y
      	schShape = "ltccother"
      )

      layShape = schShape
	
; If T = "rect", parameters are SC, WC, NOT, X, Y
; If T = "oct" or "cir", parameters are SC, WC, NOT, OD

      rect = nil
      diffi = nil
      octdiffi = nil
      ltccrect = nil
      ltccother = nil
      when( layShape == "rect" rect = t  )
      when( layShape == "diffi" diffi = t  )
      when( layShape == "octdiffi" octdiffi = t  )
      when( layShape == "ltccrect" ltccrect = t  )
      when( layShape == "ltccother" ltccother = t  )

      if( rect then
	foreach( prop '( Track_Distance Track_Size Turns Size_X Size_Y)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
      );if rect

      if( diffi then
      	foreach( prop '( Track_Size Track_Distance Turns Size_X Size_Y Int_Terminal)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
      )

      if( octdiffi then
      	foreach( prop '( Track_Size Track_Distance Turns Size_X Int_Terminal)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
      )
		
      if( !rect && !diffi && !octdiffi && !ltccother && !ltccrect then
      	foreach( prop '( Track_Distance Track_Size Turns Size_X)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
      ) ;if !rect && !diffi && !ltccrect && !ltccother
      
      if( ltccrect then
      	foreach( prop '( Track_Distance Track_Size Turns Size_X Size_Y Layer_Name)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
      ) ;if ltccrect

      if( ltccother then
      	foreach( prop '( Track_Distance Track_Size Turns Size_X Layer_Name)
          unless( get( layPlist prop )
            missingLay = cons( prop missingLay )
          ) ;unless
          unless( get( schPlist prop )
            missingSch = cons( prop missingSch )
          ) ;unless
	) ;foreach
	if((length(missingLay)==1 && layPlist->Turns==1.0) then
		layPlist->Track_Distance = 80.0
		missingLay = nil
		)
      );if ltccrect

  ; Report all missing parameters

      when( ( missingLay || missingSch )

        errorCount = errorCount + 1
        printf( "\n" )
        printf( "** Parameter Error # %g \n" float(errorCount) )
        printf( "** Matched inductor with missing parameters:\n" )

	when( missingSch
	  sprintf( msg1 "Sch %s Ind missing params: %s" 
	    schShape buildString( missingSch ",") )
          printf( 
            "   Schematic: %s Inductor missing parameters: %s \n" 
            schShape buildString( missingSch ",") )
	) ;when missingSch

	when( !missingSch

	  when( rect
	    sprintf( msg1 "Sch rect Ind WC/SC/NOT/X/Y %g %g %g %g %g"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y)
            )
          )

	  when( diffi
	    sprintf( msg1 "Sch SquDiff Ind WC/SC/NOT/X/Y/CT %g %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y) schPlist->Int_Terminal
            )
          )

	  when( octdiffi
	    sprintf( msg1 "Sch OctDiff Ind WC/SC/NOT/X/CT %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) schPlist->Int_Terminal
            )
          )

	  when( !rect && !diffi && !octdiffi && !ltccother && !ltccrect
	    sprintf( msg1 "Sch %s Ind WC/SC/NOT/OD %g %g %g %g"
		schShape float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X)
            )
          )

	  when( ltccrect
	    sprintf( msg1 "Sch LTCCsquare Ind WC/SC/NOT/X/Y/Layer %g %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y) schPlist->Layer_Name
            )
          )

	  when( ltccother
	    sprintf( msg1 "Sch LTCCsquare Ind WC/SC/NOT/X/Layer %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) schPlist->Layer_Name
            )
          )

          printf( "   Schematic: %s Inductor with all parameters \n" schShape )

	  when( rect
	    printf( 
              "     WC = %g SC = %g NOT = %g X = %g Y = %g\n"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y)
            )
          ) ;when

	  when( diffi
            printf(
              "     WC = %g SC = %g NOT = %g X = %g Y = %g CT = %s\n"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y) schPlist->Int_Terminal
            )
          ) ;when

	  when( octdiffi
            printf(
              "     WC = %g SC = %g NOT = %g X = %g CT = %s\n"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) schPlist->Int_Terminal
            )
          ) ;when

	  when( !rect && !diffi && !octdiffi && !ltccother && !ltccrect
            printf(
              "     WC = %g SC = %g NOT = %g OD = %g \n" 
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) 
            )
          ) ;when

	  when( ltccrect
	    sprintf( msg1 "Sch LTCCsquare Ind WC/SC/NOT/X/Y/Layer %g %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) float(schPlist->Size_Y) schPlist->Layer_Name
            )
          )

	  when( ltccother
	    sprintf( msg1 "Sch LTCCsquare Ind WC/SC/NOT/X/Layer %g %g %g %g %s"
		float(schPlist->Track_Size) float(schPlist->Track_Distance)
		float(schPlist->Turns) float(schPlist->Size_X) schPlist->Layer_Name
            )
          )

	) ;when !missingSch

	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)

	when( missingLay 
	  sprintf( msg2 "Lay %s Ind missing params: %s"
	    layShape buildString( missingLay ",") )
          printf(
            "   Layout: %s Inductor missing parameters: %s \n" 
            layShape buildString( missingLay ",") )

	) ;when missingLay

	when( !missingLay

	  when( rect
	    sprintf( msg2 "Lay rect Ind WC/SC/NOT/X/Y %g %g %g %g %g"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Size_Y
            )
          )

	  when( diffi
	    sprintf( msg2 "Lay SquDiff Ind WC/SC/NOT/X/Y/CT %g %g %g %g %g %s "
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Size_Y layPlist->Int_Terminal
            )
          )

	  when( octdiffi
	    sprintf( msg2 "Lay OctDiff Ind WC/SC/NOT/X/CT %g %g %g %g %s "
              float(layPlist->Track_Size) float(layPlist->Track_Distance)
              float(layPlist->Turns) float(layPlist->Size_X) layPlist->Int_Terminal
            )
          )

	  when( !rect && !diffi && !octdiffi && !ltccother && !ltccrect
	    sprintf( msg2 "Lay %s Ind WC/SC/NOT/OD %g %g %g %g"
              layShape layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X
            )
          )

	  when( ltccrect
	    sprintf( msg2 "Lay rect Ind WC/SC/NOT/X/Y/Layer %g %g %g %g %g %s"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Size_Y layPlist->Layer_Name
            )
          )

	  when( ltccother
	    sprintf( msg2 "Lay rect Ind WC/SC/NOT/X/Layer %g %g %g %g %s"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Layer_Name
            )
          )

          printf( "   Layout: %s Inductor with all parameters \n" layShape )

	  when( diffi
            printf(
              "     WC = %g SC = %g NOT = %g X = %g Y = %g CT = %s\n"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Size_Y layPlist->Int_Terminal
            )
          ) ;when

	  when( octdiffi
            printf(
              "     WC = %g SC = %g NOT = %g X = %g CT = %s\n"
              float(layPlist->Track_Size) float(layPlist->Track_Distance)
              float(layPlist->Turns) float(layPlist->Size_X) layPlist->Int_Terminal
            )
          ) ;when

	  when( !rect && !diffi && !octdiffi && !ltccother && !ltccrect
            printf(
              "     WC = %g SC = %g NOT = %g OD = %g \n" 
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X
            )
          ) ;when

	  when( ltccrect
	    sprintf( msg2 "Lay rect Ind WC/SC/NOT/X/Y/Layer %g %g %g %g %g %s"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Size_Y layPlist->Layer_Name
            )
          )

	  when( ltccother
	    sprintf( msg2 "Lay rect Ind WC/SC/NOT/X/Layer %g %g %g %g %s"
              layPlist->Track_Size layPlist->Track_Distance
              layPlist->Turns layPlist->Size_X layPlist->Layer_Name
            )
          )

	) ;when !missingLay

	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf( msg "Err %g: %s ; %s" 
          float(errorCount) msg1 msg2 )
        return( msg )

      ) ;when( ( missingLay || missingSch )


; now we know that all parameters are defined for both plists

      layWC  = float( layPlist->Track_Size );WC )
      laySC  = float( layPlist->Track_Distance );SC )
      layNOT = float( layPlist->Turns );NOT )

      schWC  = float( schPlist->Track_Size );WC )
      schSC  = float( schPlist->Track_Distance );SC )
      schNOT = float( schPlist->Turns );NOT )

      when( rect || diffi
	layOD = 1.0
	schOD = 1.0
	schX  = float( schPlist->Size_X );X )
  	schY  = float( schPlist->Size_Y );Y )
	layX  = float( layPlist->Size_X );X )
	layY  = float( layPlist->Size_Y );Y )
	;if( schX>schY && (schX==layY || schY==layX) then
	if( (schX>schY && layX<layY) || (schX<schY && layX>layY) then
		layX  = float( layPlist->Size_Y )
		layY  = float( layPlist->Size_X )
	)
      )
    
      when( !rect && !diffi && !ltccother && !ltccrect
	layX  = 1.0
	layY  = 1.0
	schX  = 1.0
	schY  = 1.0
	layOD = float( layPlist->Size_X )
	schOD = float( schPlist->Size_X )

      )

      when( ltccrect
	layOD = 1.0
	schOD = 1.0
	schX  = float( schPlist->Size_X );X )
  	schY  = float( schPlist->Size_Y );Y )
	layX  = float( layPlist->Size_X );X )
	layY  = float( layPlist->Size_Y );Y )
	if( (schX>schY && layX<layY) || (schX<schY && layX>layY) then
		layX  = float( layPlist->Size_Y )
		layY  = float( layPlist->Size_X )
	)
	layLN = "ltcc1"
	schLN = "ltcc1"
	schLN  =  schPlist->Layer_Name
	layLN  =  layPlist->Layer_Name

      )

      when( ltccother
	layX  = 1.0
	layY  = 1.0
	schX  = 1.0
	schY  = 1.0
	layOD = float( layPlist->Size_X )
	schOD = float( schPlist->Size_X )
	layLN = "ltcc1"
	schLN = "ltcc1"
	schLN  = schPlist->Layer_Name
	layLN  = layPlist->Layer_Name
      )

      diffWC  = 100 * (abs( layWC - schWC ) / schWC )
      diffSC  = 100 * (abs( laySC - schSC ) / schSC )
      if( ltccother && layNOT==1.0 then
      		diffSC = tolSC)
		
      diffNOT = 100 * (abs( layNOT - schNOT ) / schNOT )

      diffX   = 100 * (abs( layX - schX ) / schX )
      diffY   = 100 * (abs( layY - schY ) / schY )
      diffOD  = 100 * (abs( layOD - schOD ) / schOD )

      SCerr   = nil
      WCerr   = nil
      NOTerr  = nil
      Xerr    = nil
      Yerr    = nil
      ODerr   = nil
      LNerr   = nil

      when( diffWC  > tolWC  WCerr = t )
      when( diffSC  > tolSC  SCerr = t )
      when( diffNOT > tolNOT NOTerr = t )
      when( diffX   > tolX   Xerr = t )
      when( diffY   > tolY   Yerr = t )
      when( diffOD  > tolOD  ODerr = t )
      if(ltccrect || ltccother
      when( layLN!=schLN LNerr = t))

; if no errors return nil

      when( 
	!NOTerr && !SCerr && !WCerr && !Xerr && !Yerr && !ODerr && !LNerr
	return(nil) 
      )

; now we know there is a parameter error

  errorCount = errorCount + 1
      printf( "\n" )
      printf( "** Parameter Error # %g \n" float(errorCount) )

      when( rect

	printf( "   rect Inductor parameter mismatch \n" )

	printf(
	  "   Schematic WC = %g SC = %g NOT = %g X = %g Y = %g \n"
          schWC schSC schNOT schX schY
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf(
	  "   Layout WC = %g SC = %g NOT = %g X = %g Y = %g \n"
          layWC laySC layNOT layX layY
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf(
          str1
          "Sch T/WC/SC/NOT/X/Y %s %g %g %g %g %g"
          schShape schWC schSC schNOT schX schY
        )
        sprintf(
          str2
          "Lay T/WC/SC/NOT/X/Y %s %g %g %g %g %g"
          layShape layWC laySC layNOT layX layY
        )

      ) ;when rect 

     when( diffi

	if( schPlist->Int_Terminal == 'Off then sh = "DiffSpiral"
	else  sh = "DiffCTSpiral"
	)
	printf( "   %s Inductor parameter mismatch \n" sh)
	printf(
	  "   Schematic WC = %g SC = %g NOT = %g X = %g Y = %g \n"
          schWC schSC schNOT schX schY
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf(
	  "   Layout WC = %g SC = %g NOT = %g X = %g Y = %g \n"
          layWC laySC layNOT layX layY
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)
	sprintf(
          str1
          "Sch WC/SC/NOT/X/Y %s %g %g %g %g %g"
          sh schWC schSC schNOT schX schY
        )
        sprintf(
          str2
          "Lay WC/SC/NOT/X/Y %s %g %g %g %g"
          sh layWC laySC layNOT layX layY
        )

      ) ;when diffi

      when( octdiffi

	if( schPlist->Int_Terminal == 'Off then sh = "DiffOctSpiral"
	else  sh = "DiffCTOctSpiral"
	)
	printf( "   %s Inductor parameter mismatch \n" sh)
	printf(
	  "   Schematic WC = %g SC = %g NOT = %g X = %g \n"
          schWC schSC schNOT schX
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf(
	  "   Layout WC = %g SC = %g NOT = %g X = %g \n"
          layWC laySC layNOT layX
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf(
          str1
          "Sch WC/SC/NOT/X %s %g %g %g %g"
          sh schWC schSC schNOT schX
        )
        sprintf(
          str2
          "Lay WC/SC/NOT/X %s %g %g %g %g"
          sh layWC laySC layNOT layX
        )

      ) ;when octdiffi

      when( !rect && !diffi && !octdiffi && !ltccother && !ltccrect

	printf( "   %s Inductor parameter mismatch \n" schShape )

	printf(
	  "   Schematic WC = %g SC = %g NOT = %g OD = %g \n" 
          schWC schSC schNOT schOD
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf( 
	  "   Layout WC = %g SC = %g NOT = %g OD = %g \n"
          layWC laySC layNOT layOD
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf(
          str1
          "Sch T/WC/SC/NOT/OD %s %g %g %g %g"
          schShape schWC schSC schNOT schOD
        )
        sprintf(
          str2
          "Lay T/WC/SC/NOT/OD %s %g %g %g %g"
          layShape layWC laySC layNOT layOD
        )

      ) ;when( !rect && !diffi )

      when( ltccrect

	printf( "   LTCCsquare Inductor parameter mismatch \n" )

	printf(
	  "   Schematic WC = %g SC = %g NOT = %g X = %g Y = %g Layer = %s\n"
          schWC schSC schNOT schX schY schLN
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf(
	  "   Layout WC = %g SC = %g NOT = %g X = %g Y = %g Layer = %s\n"
          layWC laySC layNOT layX layY layLN
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf(
          str1
          "Sch T/WC/SC/NOT/X/Y/Layer %s %g %g %g %g %g %s"
          schShape schWC schSC schNOT schX schY schLN
        )
        sprintf(
          str2
          "Lay T/WC/SC/NOT/X/Y/Layer %s %g %g %g %g %g %s"
          layShape layWC laySC layNOT layX layY layLN
        )

      ) ;when ltccrect 

      when( ltccother

	printf( "   LTCC Inductor parameter mismatch \n" )

	printf(
	  "   Schematic WC = %g SC = %g NOT = %g X = %g Layer = %s\n"
          schWC schSC schNOT schOD schLN
        )
	when( schx && schy
	  printf( "     Location: x = %g y = %g \n" schx schy )
	)
	printf(
	  "   Layout WC = %g SC = %g NOT = %g X = %g Layer = %s\n"
          layWC laySC layNOT layOD layLN
        )
	when( layx && layy
	  printf( "     Location: x = %g y = %g \n" layx layy )
	)

        sprintf(
          str1
          "Sch T/WC/SC/NOT/X/Layer %s %g %g %g %g %s"
          schShape schWC schSC schNOT schOD schLN
        )
        sprintf(
          str2
          "Lay T/WC/SC/NOT/X/Layer %s %g %g %g %g %s"
          layShape layWC laySC layNOT layOD layLN
        )

      ) ;when ltccother 


      sprintf(
        msg "Err %g: %s; %s"
        float(errorCount) str1 str2
      )

      return( msg )

   ) ; end prog
  ) ; end compareINDVeloceRF


; Compare varactor properties

  procedure( compareJVF( layPlist, schPlist )
    prog( (missingLay missingSch)

; If nf is missing generate a parameter mismatch

      foreach( prop '( nf )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch xjvar_w40 missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch xjvar_w40 %g"
            float(schPlist->nf)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay xjvar_w40 missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay xjvar_w40 %g"
            float(layPlist->nf)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that nf are defined for both plists

      layJVF   = float( layPlist->nf  )

      schJVF   = float( schPlist->nf )

      JVFerr  = nil

      when( layJVF   != schJVF JVFerr = t )

; if no errors return nil

      when( !JVFerr return(nil) )

; now we know there is a parameter error

      sprintf( msg "Err: xjvar_w40 Sch nf %g;
        Lay nf %g"
        schJVF
        layJVF)
      return( msg )

   ) ; end prog
  ) ; end compareJVF

; *****************************************************************
; Compare varactor properties

  procedure( compareJVW( layPlist, schPlist )
    prog( (missingLay missingSch)

; If w is missing generate a parameter mismatch

      foreach( prop '( w )
        unless( get( layPlist prop )
          missingLay = cons( prop missingLay )
        ) ;unless
        unless( get( schPlist prop )
          missingSch = cons( prop missingSch )
        ) ;unless
      ) ;foreach

; Report all missing parameters

      when( ( missingLay || missingSch )

        when( missingSch
          sprintf( msg1 "Sch xjvar_nf36 missing params: %s"
            buildString( missingSch ",") )
        ) ;when
        when( !missingSch
          sprintf( msg1 "Sch xjvar_nf36 %g"
            float(schPlist->w)
          )
        ) ;when

        when( missingLay
          sprintf( msg2 "Lay xjvar_nf36 missing params: %s"
            buildString( missingLay ",") )
        ) ;when
        when( !missingLay
          sprintf( msg2 "Lay xjvar_nf36 %g"
            float(layPlist->w)
          )
        ) ;when

        sprintf( msg "Err: %s ; %s" msg1 msg2 )
        return( msg )
      )

; now we know that w are defined for both plists

      layJVW   = float( layPlist->w  )

      schJVW   = float( schPlist->w )

      JVWerr  = nil

      when( layJVW   != schJVW JVWerr = t )

; if no errors return nil

      when( !JVWerr return(nil) )

; now we know there is a parameter error

      sprintf( msg "Err: xjvar_nf36 Sch w %g;
        Lay w %g"
        schJVW
        layJVW)
      return( msg )

   ) ; end prog
  ) ; end compareJVW

procedure( comparePCM( layPlist schPlist )
 prog( ( missingLay missingSch tmp propNameList lval sval tol errorCount msg )

	errorCount = 0
	msg = ""

	;;; Determine tolerance for numerical comparisons
	unless( tol = schPlist->tol
	    tol = 1.0 ;;; percent
	)

	;;; Get a list of properties to check
	;;; Look for a list of parameters on the schematic instance. If none
	;;; then only check the mnodel name
	unless( propNameList = schPlist->auLvsCompareParamList
	    propNameList = "model"
	)
	propNameList = parseString( propNameList "," )

	;;; Check each property one at a time
	foreach( prop propNameList
	    if( sval = get( schPlist prop ) then
		if( lval = get( layPlist prop ) then
		    unless( lval == sval
			if( and( numberp(lval) numberp(sval) ) then
			    lval=float(lval)
			    sval=float(sval)
			    when( abs((lval-sval)/sval*100.0) > tol
				sprintf( msg "%sParam. mismatch: %s Sch=%g Lay=%g \n"
				    msg prop sval lval
				)
				++errorCount
			    )
			else
			    sprintf( msg "%sParam. mismatch: %s Sch=%g Lay=%g\n"
				msg prop sval lval
			    )
			    ++errorCount
			)
		    )
		else
		    missingLay = cons( prop missingLay )
		    ++errorCount
		)
	    else
		    missingSch = cons( prop missingSch )
		    ++errorCount
	    )
	)

	when( missingLay
	    sprintf( msg "Missing parameters on layout: `%s' \n"
		buildString( missingLay "," )
	    )
	)
	when( missingSch
	    sprintf( msg "Missing parameters on schematic: `%s' \n"
		buildString( missingSch "," )
	    )
	)

	when( strlen(msg) == 0
	    msg = nil
	)

	return(msg)
    )
)

; *********************************************************************
; Call the procedures
; *********************************************************************

; Device combining

  mergeParallel( "ind" noMerge )
  mergeParallel( "mimcap" merge parallelMIMCAP )
  mergeParallel( "ndio" merge parallelDIO )
  mergeParallel( "nmos" merge parallelMOS )
  mergeParallel( "nmoshv" merge parallelMOS )
  mergeParallel( "nmoscap" merge  parallelMOSCAP )
  mergeParallel( "pmoscap" merge  parallelMOSCAP )
  mergeParallel( "nplusres" merge parallelRES )
  mergeParallel( "pplusres" merge parallelRES )
  mergeParallel( "polyres" merge parallelRES )
  mergeParallel( "polyhres" merge parallelRES )
  mergeParallel( "nwellres" merge parallelRES )
  mergeParallel( "npn" merge parallelBJT )
  mergeParallel( "pdio" merge parallelDIO )
  mergeParallel( "pmos" merge parallelMOS )
  mergeParallel( "pmoshv" merge parallelMOS )
  mergeParallel( "pnp" merge parallelBJT )
  mergeParallel( "vpnp" merge parallelBJT )

  mergeSeries( "ind" noMerge )
  mergeSeries( "mimcap" noMerge )
  mergeSeries( "ndio" noMerge )
  mergeSeries( "nmos" noMerge )
  mergeSeries( "nmoshv" noMerge )
  mergeSeries( "nmoscap" noMerge )
  mergeSeries( "pmoscap" noMerge )
  mergeSeries( "nplusres" merge seriesRES )
  mergeSeries( "pplusres" merge seriesRES )
  mergeSeries( "polyres" merge seriesRES )
  mergeSeries( "polyhres" merge seriesRES )
  mergeSeries( "nwellres" merge seriesRES )
  mergeSeries( "npn" noMerge )
  mergeSeries( "pdio" noMerge )
  mergeSeries( "pmos" noMerge )
  mergeSeries( "pmoshv" noMerge )
  mergeSeries( "pnp" noMerge )
  mergeSeries( "vpnp" noMerge )


; Uncomment the next line if you do NOT want to form
; pseudo devices out of parallel MOS configurations.

; formGate( none )


; Parameter comparison

;  compareParameter( "ind" compareIND )
  compareParameter( "mimcap" compareMIMCAP )
  compareParameter( "ndio" compareDIO )
  compareParameter( "nmos" compareMOS )
  compareParameter( "nmoshv" compareMOS )
  compareParameter( "nmoscap" compareMOSCAP )
  compareParameter( "pmoscap" compareMOSCAP )
  compareParameter( "nplusres" compareRES )
  compareParameter( "pplusres" compareRES )
  compareParameter( "polyres" compareRES )
  compareParameter( "polyhres" compareRES )
  compareParameter( "nwellres" compareRES )
  compareParameter( "npn" compareBJT )
  compareParameter( "pdio" compareDIO )
  compareParameter( "pmos" compareMOS )
  compareParameter( "pmoshv" compareMOS )
  compareParameter( "pnp" compareBJT )
  compareParameter( "vpnp" compareBJT )
  compareParameter( "xjvar_w40" compareJVF )
  compareParameter( "xjvar_nf36" compareJVW )

; VeloceRF
  compareParameter( "ind_squ" compareINDVeloceRF )
  compareParameter( "ind_oct" compareINDVeloceRF )
  compareParameter( "ind_cir" compareINDVeloceRF )
  compareParameter( "inddiff" compareINDVeloceRF )
  compareParameter( "inddiffct" compareINDVeloceRF )
  compareParameter( "octdiff" compareINDVeloceRF )
  compareParameter( "octdiffct" compareINDVeloceRF )
  compareParameter( "indxf" compareINDVeloceRF )

;;;; ******************************************************************************************
;;; VPCM BEGIN
;;;; ******************************************************************************************
  compareParameter( "pcm2" comparePCM )
  compareParameter( "pcm3" comparePCM )
  compareParameter( "pcm4" comparePCM )
  compareParameter( "pcm5" comparePCM )
  compareParameter( "pcm6" comparePCM )
  compareParameter( "SmplInd" comparePCM )
  compareParameter( "DiffInd" comparePCM )
  compareParameter( "DiffIndNoTap" comparePCM )

layout(
      ;;; Remove following filterDevice statements for graybox connectivity checking
      filterDevice( "pcm_simopen" ) 
      ;;; Filtering for dummy devices
      filterOptions( "XZ")
)
schematic(
      filterDevice( "pcm_simopen" ) 
)

;;; Regular blackbox filtering
if( avSwitch( "APL" ) then
    layout(
	filterDevice( "pcm_simshort" )
    )
    schematic(
	filterDevice( "pcm_simshort" )
	filterDevice( "SmplInd" )
	filterDevice( "DiffInd" )
	filterDevice( "DiffIndNoTap" )
	filterDevice( "pcm2" )
	filterDevice( "pcm3" )
	filterDevice( "pcm4" )
	filterDevice( "pcm5" )
    )
)

;;;; ******************************************************************************************
;;; VPCM END
;;;; ******************************************************************************************

if( avSwitch("VLC") then
  
  layout(
	filterDevice( "HC_cn" short("PLUS" "MINUS"))
	filterDevice( "ind_path" short("PLUS" "MINUS"))
	filterDevice( "short" )
	filterDevice( "TIE" )
  )
  schematic(
	filterDevice( "ind_path" short("PLUS" "MINUS"))
	filterDevice( "HC_cn" short("PLUS" "MINUS"))
	filterDevice( "short" )
	filterDevice( "TIE" )
  )

)

if( avSwitch("HF") then 

  layout(  
        filterDevice( "HC_cn" )
        filterDevice( "ind_path" )
        filterDevice( "short" )
        filterDevice( "TIE" )
        filterDevice( "ind_squ" )
        filterDevice( "ind_oct" )
        filterDevice( "ind_cir" )
        filterDevice( "inddiff" )
        filterDevice( "inddiffct" )
        filterDevice( "inddiffCT" )
        filterDevice( "octdiff" )
        filterDevice( "octdiffct" )
        filterDevice( "indxf" )

  )
  schematic( 
        filterDevice( "ind_path" )
        filterDevice( "HC_cn" )
        filterDevice( "short" )
        filterDevice( "TIE" )
        filterDevice( "ind_squ" )
        filterDevice( "ind_oct" )
        filterDevice( "ind_cir" )
        filterDevice( "inddiff" )
        filterDevice( "inddiffct" )
        filterDevice( "inddiffCT" )
        filterDevice( "octdiff" )
        filterDevice( "octdiffct" )
        filterDevice( "indxf" )

  )

)

;  layout(
;                indDevice( "ind" "PLUS" "MINUS")
;        )

) ;end avCompareRules
